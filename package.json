{
  "name": "iownthis",
  "homepage": ".",
  "dependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.3.0",
    "ethers": "^5.1.0",
    "hardhat": "^2.1.2",
    "hardhat-deploy": "^0.7.4",
    "react-helmet": "^6.1.0",
    "web3": "^1.3.5"
  },
  "scripts": {
    "heroku-postbuild": "cd client && npm install && cd ../backend/ && npm install && cd ../client && npm run build",
    "start": "node backend/src/server.js"
  },
  "engines": {
    "node": "14.x"
  }
}
