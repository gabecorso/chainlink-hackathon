import {Card} from "react-bootstrap";

export default function DocumentsTab() {
    return (
        <div className={'p-3'}>
            <Card className={'mt-3'} style={{backgroundColor: 'aliceblue'}} body>
                <h2>Upload company documents</h2>
            </Card>
        </div>
    )
}